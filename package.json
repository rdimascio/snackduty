{
  "name": "snackday",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck && bun run scripts:typecheck",
    "scripts:typecheck": "tsc --noEmit -p scripts/tsconfig.json",
    "scripts:test": "bun test scripts/lib scripts/runtime/aws-staging-preflight.test.ts",
    "check:fast": "bun run format:check && bun run lint && bun run --filter web typecheck && bun run --filter '@snackday/domain' typecheck && bun run --filter '@snackday/platform-manifest' typecheck && bun run scripts:typecheck && bun run --filter web test && bun run --filter '@snackday/domain' test && bun run --filter '@snackday/platform-manifest' test && bun run scripts:test",
    "dev:scenario": "bun scripts/dev-scenario.ts",
    "check:scenario": "bun scripts/dev-scenario.ts --check",
    "runtime:serve": "bun apps/web/runtime/server.ts",
    "runtime:verify": "bun scripts/runtime/verify-local.ts",
    "runtime:storage": "bun scripts/runtime/storage.ts",
    "runtime:probe": "bun scripts/runtime/probe-remote.ts",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "ios:build": "bash scripts/ios-build.sh",
    "ios:test": "bash scripts/ios-test.sh",
    "ios:test:ui": "bash scripts/ios-ui-test.sh",
    "ios:verify": "bash scripts/ios-verify.sh",
    "test": "bun run --filter '*' test && bun run scripts:test",
    "accept": "bun scripts/acceptance.ts",
    "gate": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run build && bun run accept"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "alchemy": "0.93.12",
    "oxfmt": "^0.54.0",
    "oxlint": "1.73.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  },
  "packageManager": "bun@1.3.5"
}
